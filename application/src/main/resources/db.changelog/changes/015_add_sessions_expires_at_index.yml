databaseChangeLog:
  - changeSet:
      id: add_sessions_expires_at_index
      author: dev
      comments: Add index on sessions.expires_at to speed up expired session cleanup queries
      changes:
        - sql:
            sql: |
              CREATE INDEX IF NOT EXISTS idx_sessions_expires_at
                ON finance_tracker.sessions(expires_at);
